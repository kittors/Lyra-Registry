{
  "name": "@lyra/registry-shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The registry contract: what an index may say, what the API answers, shared by client, worker and site",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
